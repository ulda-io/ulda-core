export const DEFAULT_ULDA_VERSION = "1";
export const DEFAULT_EXPORT_FORMAT = "hex";

export const DEFAULT_SIGN_CONFIG = Object.freeze({
  N: 5,
  mode: "S",
  hash: "SHA-256",
  originSize: 256,
  pack: "compactV1",
  ageBytes: 4
});
